{% extends 'base.html.twig' %}

{% block body %}

    <h1>{{ page.title }}</h1>
    <h4><em>Category: {{ page.category }}</em></h4>
    <p>{{ page.content }}</p>
    <a href="{{ path('custom_cms_homepage') }}">Back to the homepage</a>

{% endblock %}

